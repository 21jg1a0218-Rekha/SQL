// to create a employ table
CREATE table employe_prof
(
  sl int UNIQUE,
  sname varchar(100) NOT NULL,
  empid varchar(50),
  salary int NOT NULL,
  age int CHECK (age>=20),
  location varchar(100) DEFAULT 'hyderabad',
  PRIMARY KEY(empid)
);
